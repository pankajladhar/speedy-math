const Number = {
    Random : require('./Random').default,
    Sort : require('./Sort').default,
    RandomWithSize: require('./RandomWithSize').default,
    RandomComparator: require('./RandomComparator').default,
}

export default Number;